@model IEnumerable<DaceItFront.Models.Player>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.SteamId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NamePlayer)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PlayerMmr)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.SteamId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NamePlayer)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PlayerMmr)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdPlayer">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdPlayer">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdPlayer">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
